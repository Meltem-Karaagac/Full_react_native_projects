/**
 * @format
 */

import {AppRegistry} from 'react-native';
import MainPanel from './src/components/MainPanel';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MainPanel);
